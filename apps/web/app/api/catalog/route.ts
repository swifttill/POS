import { requirePermission } from "../../../lib/auth"; import { db } from "../../../lib/db"; import { apiError } from "../../../lib/api-error"; import { json,safe } from "../../../lib/json";
export async function GET(){try{await requirePermission("menu.view");const categories=await db.category.findMany({where:{active:true},orderBy:{displayOrder:"asc"},include:{items:{where:{active:true},orderBy:{displayOrder:"asc"},include:{variants:{where:{active:true},orderBy:{displayOrder:"asc"}},modifierLinks:{orderBy:{displayOrder:"asc"},include:{modifierGroup:{include:{options:{where:{active:true},orderBy:{displayOrder:"asc"}}}}}}}}}});return json({ok:true,categories:safe(categories)})}catch(e){return apiError(e)}}
