import { requirePermission } from "../../../../lib/auth";import { db } from "../../../../lib/db";import { apiError } from "../../../../lib/api-error";import { json } from "../../../../lib/json";
export async function GET(){try{await requirePermission("users.view");const [users,roles,permissions]=await Promise.all([db.user.findMany({orderBy:{name:"asc"},select:{id:true,name:true,active:true,roles:{include:{role:true}},permissionOverrides:{include:{permission:true}}}}),db.role.findMany({orderBy:{name:"asc"},include:{permissions:{include:{permission:true}},_count:{select:{users:true}}}}),db.permission.findMany({orderBy:{key:"asc"}})]);return json({ok:true,users,roles,permissions})}catch(e){return apiError(e)}}
