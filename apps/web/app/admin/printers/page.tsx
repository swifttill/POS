export default function PrintersPage(){return <main className="ops-workspace"><header className="ops-header"><div><p className="eyebrow">SwiftTill Back Office</p><h1>Printers & Cash Drawer</h1><p>Windows USB receipt-printer configuration for this POS workstation.</p></div><a className="primary-action" href="/pos/receipts">Receipts</a></header><section className="ops-toolbar"><button>Discover Windows Printers</button><button disabled>Print Test Page</button><button disabled>Open Drawer</button></section><section className="ops-grid"><div className="ops-list"><div className="empty-state"><strong>No local printer connected</strong><span>Install and start SwiftTill Print Service on this Windows POS. Printer discovery comes from the local service; no demo devices are shown.</span></div></div><aside className="ops-detail"><h2>Direct USB</h2><p>Normal flow: SwiftTill → localhost print service → Windows installed USB printer → ESC/POS RAW output.</p><p>Payment success is independent of printer status. Failed jobs remain retryable.</p><div className="receipt-paper"><strong>Security</strong><p>Loopback-only service + workstation print token. LAN/cloud printer routing is not enabled in V1.</p></div></aside></section></main>}
