"use client";
import type { MenuItemDTO } from "@/lib/types";
import { formatPaisa } from "@/lib/money";
export function MenuItemCard({item,onClick}:{item:MenuItemDTO;onClick:(item:MenuItemDTO)=>void}){
 return <button type="button" onClick={()=>onClick(item)} disabled={!item.available} className="group text-left bg-white border border-line rounded-2xl overflow-hidden hover:border-brand/35 hover:shadow-[0_12px_28px_rgba(17,24,39,.08)] hover:-translate-y-0.5 transition active:translate-y-0 disabled:opacity-50 disabled:translate-y-0"><div className="relative aspect-[1.45/1] bg-panel overflow-hidden">{item.imageUrl?<img src={item.imageUrl} alt={item.name} className="w-full h-full object-cover group-hover:scale-[1.035] transition duration-300"/>:<div className="w-full h-full flex flex-col items-center justify-center text-muted bg-gradient-to-br from-surface-2 to-panel"><div className="w-9 h-9 rounded-xl bg-white border border-line flex items-center justify-center font-extrabold text-brand">{item.name.slice(0,1).toUpperCase()}</div><span className="text-[10px] mt-2">No photo</span></div>}<div className="absolute top-2 right-2 rounded-lg bg-white/92 backdrop-blur px-2 py-1 text-[11px] font-extrabold text-ink shadow-sm">{formatPaisa(item.price)}</div>{!item.available?<span className="absolute inset-0 bg-ink/62 text-white text-xs font-bold flex items-center justify-center">Unavailable</span>:null}</div><div className="p-3.5"><div className="font-extrabold text-[14px] leading-5 line-clamp-2 min-h-10 text-ink">{item.name}</div>{item.description?<div className="text-[11px] text-muted line-clamp-1 mt-1.5">{item.description}</div>:<div className="text-[11px] text-muted/70 mt-1.5">Ready to add</div>}<div className="flex items-center justify-between mt-3"><span className="text-[10px] font-bold uppercase tracking-wider text-muted">{item.modifierGroups.length?"Customisable":"Quick add"}</span><span className="w-7 h-7 rounded-lg bg-brand-soft text-brand flex items-center justify-center font-bold group-hover:bg-brand group-hover:text-white transition">+</span></div></div></button>
}
