"use client";
import type { CategoryDTO } from "@/lib/types";
export function CategoryRail({categories,activeId,onSelect,showDeals,dealsActive,onSelectDeals}:{categories:CategoryDTO[];activeId:string|null;onSelect:(id:string)=>void;showDeals?:boolean;dealsActive?:boolean;onSelectDeals?:()=>void}){
 const base="shrink-0 h-11 px-4 rounded-xl border text-sm font-bold flex items-center gap-2.5 transition";
 return <div className="flex gap-2 overflow-x-auto pb-1.5 pos-scroll">{showDeals?<button onClick={onSelectDeals} className={`${base} ${dealsActive?"bg-ink text-white border-ink shadow-sm":"bg-white border-line text-text hover:border-brand/35 hover:bg-brand-soft/25"}`}><span className="w-2 h-2 rounded-full bg-brand"/>Deals</button>:null}{categories.map(c=><button key={c.id} onClick={()=>onSelect(c.id)} className={`${base} ${activeId===c.id?"bg-brand text-white border-brand shadow-[0_5px_14px_rgba(255,121,0,.18)]":"bg-white border-line text-text hover:border-brand/35 hover:bg-brand-soft/25"}`}>{c.imageUrl?<img src={c.imageUrl} alt="" className="w-6 h-6 rounded-lg object-cover border border-black/5"/>:<span className={`w-6 h-6 rounded-lg flex items-center justify-center text-[10px] font-extrabold ${activeId===c.id?"bg-white/15":"bg-surface-2 text-muted"}`}>{c.name.slice(0,1).toUpperCase()}</span>}<span>{c.name}</span><span className={`text-[10px] ${activeId===c.id?"text-white/65":"text-muted"}`}>{c.items.length}</span></button>)}</div>
}
