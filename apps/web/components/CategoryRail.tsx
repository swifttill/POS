"use client";
import type { CategoryDTO } from "@/lib/types";
export function CategoryRail({ categories, activeId, onSelect, showDeals, dealsActive, onSelectDeals }: { categories: CategoryDTO[]; activeId: string | null; onSelect: (id: string) => void; showDeals?: boolean; dealsActive?: boolean; onSelectDeals?: () => void }) {
  return <div className="flex gap-2 overflow-x-auto pb-1">{showDeals ? <button onClick={onSelectDeals} className={`shrink-0 h-10 px-4 rounded-lg border text-sm font-semibold ${dealsActive ? "bg-brand text-white border-brand" : "bg-white border-line text-text hover:bg-surface-2"}`}>Deals</button> : null}{categories.map(c => <button key={c.id} onClick={() => onSelect(c.id)} className={`shrink-0 h-10 px-4 rounded-lg border text-sm font-semibold flex items-center gap-2 ${activeId === c.id ? "bg-brand text-white border-brand" : "bg-white border-line text-text hover:bg-surface-2"}`}>{c.imageUrl ? <img src={c.imageUrl} alt="" className="w-5 h-5 rounded object-cover"/> : null}<span>{c.name}</span><span className="opacity-65 text-xs">{c.items.length}</span></button>)}</div>;
}
