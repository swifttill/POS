# Copy to .env locally. Never commit real values.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/swifttill"
SESSION_SECRET="replace-with-a-long-random-secret"
# First-run only. Remove these values after `pnpm bootstrap:admin` succeeds.
SWIFTTILL_BOOTSTRAP_ADMIN_NAME=""
SWIFTTILL_BOOTSTRAP_ADMIN_PIN=""
